import { SearchAccnoPipe } from './search-accno.pipe';

describe('SearchAccnoPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchAccnoPipe();
    expect(pipe).toBeTruthy();
  });
});
