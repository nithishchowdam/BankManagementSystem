import { SearchTranIdPipe } from './search-tran-id.pipe';

describe('SearchTranIdPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchTranIdPipe();
    expect(pipe).toBeTruthy();
  });
});
