import { SearchTranAccnoPipe } from './search-tran-accno.pipe';

describe('SearchTranAccnoPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchTranAccnoPipe();
    expect(pipe).toBeTruthy();
  });
});
